# API backend URL (use your machine's LAN IP for physical devices)
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000

# HMAC API secret — must match backend HMAC_API_SECRET
EXPO_PUBLIC_HMAC_API_SECRET=change-me-api-hmac-secret
